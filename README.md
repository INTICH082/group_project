# Telegram Bot для системы тестирования

Telegram-клиент для системы онлайн-тестирования с поддержкой авторизации через несколько провайдеров, управлением курсами, тестами и вопросами.

## 🌟 Особенности

- 🔐 **Многофакторная авторизация** через Code, GitHub или Яндекс ID
- 👥 **Ролевая модель** (студент/преподаватель)
- 📚 **Управление курсами** (создание, редактирование, запись студентов)
- 🧪 **Система тестирования** с вопросами и автоматической проверкой
- 📊 **Статистика и аналитика** результатов тестов
- 🐳 **Docker-контейнеризация** для простого развертывания
- 🩺 **Health-check эндпоинты** для мониторинга
- 🗄️ **Redis-кэширование** сессий пользователей

## 🛠 Технологический стек

- **Python 3.11+** с асинхронным программированием
- **Aiogram 3.x** для работы с Telegram Bot API
- **Redis** для хранения сессий и кэширования
- **Docker & Docker Compose** для контейнеризации
- **JWT** для безопасной аутентификации
- **AioHTTP** для HTTP-запросов

## 📁 Структура проекта

```
telegram-bot/
├── bot.py                 # Основной файл бота
├── requirements.txt       # Зависимости Python
├── .env.example          # Пример переменных окружения
├── Dockerfile            # Конфигурация Docker
├── docker-compose.yml    # Docker Compose для продакшена
├── docker-compose.fixed.yml  # Docker Compose для разработки
├── start.bat             # Скрипт запуска для Windows
└── README.md             # Этот файл
```

## 🚀 Быстрый старт

### Предварительные требования

- Python 3.11+
- Docker и Docker Compose (для контейнеризации)
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))
- Redis (можно запустить через Docker)

### Локальная установка (без Docker)

1. **Клонируйте репозиторий:**
   ```bash
   git clone <ваш-репозиторий>
   cd telegram-bot
   ```

2. **Создайте виртуальное окружение:**
   ```bash
   python -m venv venv
   source venv/bin/activate  # Linux/Mac
   # или
   venv\Scripts\activate     # Windows
   ```

3. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

4. **Настройте переменные окружения:**
   ```bash
   cp .env.example .env
   # Отредактируйте .env файл, добавив ваш TELEGRAM_BOT_TOKEN
   ```

5. **Запустите Redis (если не установлен локально):**
   ```bash
   docker run -d -p 6379:6379 redis:7-alpine
   ```

6. **Запустите бота:**
   ```bash
   python bot.py
   ```

### Запуск через Docker

1. **Настройте переменные окружения:**
   ```bash
   cp .env.example .env
   # Отредактируйте .env файл
   ```

2. **Запустите с Docker Compose:**
   ```bash
   docker-compose up -d
   ```

3. **Для Windows используйте готовый скрипт:**
   ```bash
   start.bat
   ```

## ⚙️ Конфигурация

### Основные переменные окружения (.env)

```env
# Telegram Bot Token (получить у @BotFather)
TELEGRAM_BOT_TOKEN=ваш_токен

# Redis URL (для кэширования и сессий)
REDIS_URL=redis://localhost:6379/0

# API Base URL
API_BASE_URL=https://my-app-logic.onrender.com

# Auth Service URL
AUTH_SERVICE_URL=http://10.197.214.4:8083/health

# JWT Secret (минимум 32 символа)
JWT_SECRET=ваш_секретный_ключ

# HTTP Port for health checks
HTTP_PORT=8081

# Default Course ID
DEFAULT_COURSE_ID=1
```

## 📖 Использование бота

### Основные команды

| Команда | Описание | Доступ |
|---------|----------|---------|
| `/start` | Начало работы | Все |
| `/login` | Вход в систему | Все |
| `/logout` | Выход из системы | Авторизованные |
| `/tests` | Список доступных тестов | Авторизованные |
| `/profile` | Ваш профиль | Авторизованные |
| `/help` | Справка по командам | Все |
| `/status` | Статус системы | Все |

### Команды для студентов

| Команда | Описание |
|---------|----------|
| `/my_courses` | Мои курсы |
| `/my_grades` | Мои оценки |
| `/my_attempts` | Мои попытки |
| `/start_test ID` | Начать тест |
| `/finish_test ID` | Завершить тест |

### Команды для преподавателей

| Команда | Описание |
|---------|----------|
| `/users` | Список пользователей |
| `/user_info ID` | Информация о пользователе |
| `/all_courses` | Все курсы |
| `/create_course` | Создать курс |
| `/course_info ID` | Информация о курсе |
| `/course_students ID` | Студенты курса |
| `/enroll_student` | Записать студента на курс |
| `/questions_list` | Список вопросов |
| `/create_question` | Создать вопрос |
| `/test_results ID` | Результаты теста |

### Команды для тестирования

| Команда | Описание |
|---------|----------|
| `/auth_student` | Авторизация как студент (без ввода кода) |
| `/auth_teacher` | Авторизация как преподаватель (без ввода кода) |
| `/simulate_auth` | Имитация веб-авторизации |
| `/debug` | Отладочная информация |
| `/services` | Информация о сервисах |
| `/ping` | Проверка работы бота |

## 🩺 Health-check и мониторинг

Бот предоставляет HTTP-эндпоинты для мониторинга:

- `http://localhost:8081/health` - Health check (JSON)
- `http://localhost:8081/status` - Статус системы (JSON)
- `http://localhost:8081/` - Информационная страница (HTML)

Пример ответа health-check:
```json
{
  "status": "healthy",
  "service": "telegram-bot",
  "timestamp": "2024-01-01T12:00:00.000Z",
  "redis": "connected",
  "active_users": 5,
  "commands_processed": 123
}
```

## 🔧 Разработка

### Запуск в режиме разработки

1. **Используйте Docker Compose для разработки:**
   ```bash
   docker-compose -f docker-compose.fixed.yml up --build
   ```

2. **Локальная разработка с hot-reload:**
   Установите `uvicorn` и запустите:
   ```bash
   uvicorn bot:main --reload --host 0.0.0.0 --port 8081
   ```

### Архитектура

- **HybridAuthService** - Гибридный сервис авторизации с поддержкой реального сервиса и заглушки
- **APIClient** - Клиент для работы с основным API системы
- **DataStorage** - Локальное хранилище данных для тестирования
- **SimpleRedis** - Адаптер Redis с fallback на локальное хранилище
- **Statistics** - Система сбора статистики


## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для вашей функции (`git checkout -b feature/amazing-feature`)
3. Зафиксируйте изменения (`git commit -m 'Add amazing feature'`)
4. Запушите в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи бота: `docker-compose logs telegram-bot`
2. Убедитесь, что Redis запущен
3. Проверьте корректность токена бота
4. Откройте Issue в репозитории

---

⭐ **Если этот проект был полезен, поставьте звезду на GitHub!**
```
