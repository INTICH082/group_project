# 🧠 Main Testing Module

Минимально функциональный модуль для создания, выдачи и проверки тестов.  
Полностью соответствует ТЗ. Никаких излишеств — только суть.

---

## 📌 Функционал

- **GetQuestion(id)** — возвращает вопрос по уникальному ID.
- **GetAnswers(questionID)** — возвращает все варианты ответов (например, 4) для заданного вопроса.
- **TakeUserAnswer(userID, questionID, answerID)** — принимает ответ пользователя и проверяет его на правильность.
- **TrackProgress(userID)** — отслеживает, какие тесты прошёл пользователь и их результаты.
- Поддержка ролей через JWT (student / teacher / admin), но **авторизация обрабатывается внешним модулем** — твой модуль только использует `user_id` и `role` из токена.

> 🔹 Админы и учителя могут **создавать/удалять вопросы**.  
> 🔹 Студенты могут **только проходить тесты** из назначенных курсов.  
> 🔹 Курсы содержат списки ID вопросов; вопросы хранятся в общей коллекции.
